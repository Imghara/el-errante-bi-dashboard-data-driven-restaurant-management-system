# ============================================================
# PYTEST CONFIG — Proyecto "El Errante" (BI & Incentivos)
# Fase 6 · Pruebas automatizadas (CP6)
# Generado: 2026-08-12
# Ejecución: .venv/Scripts/python.exe -m pytest
# ============================================================

[pytest]
testpaths = tests
addopts = -q --no-header

markers =
    e2e: pruebas integrales pesadas (navegación completa de la app con AppTest). Excluir con: -m "not e2e"
